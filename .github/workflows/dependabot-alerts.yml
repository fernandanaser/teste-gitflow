name: Dependabot

on:
  push:
    branches:
      - main

jobs:
  dependabot:
    runs-on: ubuntu-latest
    steps:
      - name: Dependabot
        uses: dependabot/dependabot-github-action@v2.5.17
        with:
          github_token: ${{ secrets.GITHUB_TOKEN }}
