name: Dependabot Alerts
on:
  push:
    branches: [ main ]
jobs:
  dependabot:
    runs-on: ubuntu-latest
    steps:
      - uses: dependabot/dependabot-core@0.215.0
        with:
          directory: "/"
          github_token: ${{ secrets.GITHUB_TOKEN }}
          ignore: "all"
